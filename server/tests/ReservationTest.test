const ReservationsController = require('../controllers/ReservationController.js')

describe('Reservations Test', function(){
    this.timeout(15000)
    // i think e7na lazem nrun el server ? :))
    // aha sa7e7
    it('Should get list of reservations', (done) => {
        // tb el request wel res ele etb3to lel controller ? 
        ReservationsController.index.then((info) => console.log(info))
    })
});

